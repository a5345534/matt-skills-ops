# Workflow home separates planning from implementation

Matt Auto keeps the clarified solution, spec, and ticket breakdown in one Workflow home session, then creates a fresh child session for each implementation and returns after its disposition. We choose this over one continuous session so planning retains its context while each ticket receives the clean context window required by the implementation flow.
